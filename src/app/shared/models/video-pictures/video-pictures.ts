export class VideoPictures {
    id: number;
    picture: string;
    nr: number;

    constructor(id: number, picture: string, nr: number) {
    this.id = id
    this.picture = picture
    this.nr = nr
    }
}